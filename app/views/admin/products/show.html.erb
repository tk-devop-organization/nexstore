<h1><%= @product.name %></h1>
<p><%= number_to_currency(@product.price) %></p>
<p><%= @product.description %></p>

<% if @product.images.attached? %>
  <div class="images">
    <% @product.images.each do |image| %>
      <%= image_tag image, size: "600x600" %>
    <% end %>
  </div>
<% end %>
